import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rubbysoft.rubbyroomchat',
  appName: 'RubbyRoomChat',
  webDir: 'build'
};

export default config;
